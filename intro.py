# Interpretator - kodni xatoliklarga tekshiruvchi dastur  
# Kompilyator - kodni mashina tiliga o'giruvchi dastur



# RAM - Random Access Memory  
# ROM - Read Only Memory 

# def main():
#     return True 

# print(main) # <function main at 0x000001B4A08404A0>

string = "Hello world" # str 
print(str)

# int - butun sonlar 
# float - qoldiqli sonlar 
# bool - True, False - Ha , Yoq 
# None - Qiymat mavjud emasligini bildiradi 
# list - ro'yhatlar, array, massiv - tartibli elementlar ketma-ketligi
# tuple - kortej - tartibli o'zgarmas elementlar ketma-ketligi
# dict - lug'at, elementlariga kalit yordamida murojaat qilinadigan obyektlar toplami
# range - sonlar diapazoni (oraligi)
# set - unikal tartibsiz elementlar to'plami 
# frozenset - unikal o'zgarmas tartibsiz elementlar to'plami

# 1. Arifmetik operatorlar
# Bu operatorlar matematik hisob-kitoblar uchun ishlatiladi:

# + (qo‘shish)
# - (ayirish)
# * (ko‘paytirish)
# / (bo‘lish)
# // (butun sonli bo‘lish)
# % (qoldiqni olish)
# ** (darajaga ko‘tarish)
# 2. Taqqoslash (mantiqiy) operatorlari
# Bu operatorlar qiymatlarni solishtirish uchun ishlatiladi:

# == (tenglik)
# != (teng emaslik)
# > (katta)
# < (kichik)
# >= (katta yoki teng)
# <= (kichik yoki teng)
# 3. Mantiqiy operatorlar
# Mantiqiy qiymatlar bilan ishlash uchun ishlatiladi:

# and (ikkalasi ham to‘g‘ri bo‘lsa)
# or (hech bo‘lmaganda bittasi to‘g‘ri bo‘lsa)
# not (inkor qilish)
# 4. Assignatsiya (berilgan) operatorlari
# Qiymatlarni o‘zgaruvchilarga tayinlash uchun ishlatiladi:

# = (oddiy tayinlash)
# += (qo‘shish va tayinlash)
# -= (ayirish va tayinlash)
# *= (ko‘paytirish va tayinlash)
# /= (bo‘lish va tayinlash)
# //= (butun sonli bo‘lish va tayinlash)
# %= (qoldiqni olish va tayinlash)
# **= (darajaga ko‘tarish va tayinlash)
# 5. Bitwise operatorlar
# Ikkinchi darajali (bitlar darajasida) operatsiyalar uchun ishlatiladi:

# & (va)
# | (yoki)
# ^ (eksklyuziv yoki)
# ~ (inkor)
# << (chapga surish)
# >> (o‘ngga surish)
# 6. Identity operatorlar
# Obyektlarning bir xil ekanligini tekshiradi:

# is (bir xil obyektmi?)
# is not (bir xil obyekt emasmi?)
# 7. Membership operatorlar
# x = 1
# y = 1
# z = 2
# print(x is not z ) # tru
# Obyektning kolleksiyalarda mavjudligini tekshiradi:

# in (ichida bor)
# not in (ichida yo‘q)

